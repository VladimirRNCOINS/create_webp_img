Мы используем Vue 2 (Options API). 
При проверке и написании кода всегда используй:
- data(), methods, computed, mounted.
- Не используй setup() и ref/computed из @vue/composition-api, если это не запрошено отдельно.
- Следи за утечками памяти при работе с изображениями.